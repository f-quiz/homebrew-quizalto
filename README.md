# Homebrew Tap for Quizalto CLI

## Install

```bash
brew tap f-quiz/quizalto
cd "$(brew --repo f-quiz/quizalto)" && git fetch origin PR-283 && git checkout PR-283
brew install quizalto
```

Branch: PR-283
