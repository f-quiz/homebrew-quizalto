# Homebrew Tap for Quizalto CLI

## CLI Install

```bash
brew tap f-quiz/quizalto
cd "$(brew --repo f-quiz/quizalto)" && git fetch origin PR-380 && git checkout PR-380
brew install quizalto
```

Branch: PR-380
