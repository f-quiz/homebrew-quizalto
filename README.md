# Homebrew Tap for Quizalto CLI

## Install

```bash
brew tap f-quiz/quizalto
cd "$(brew --repo f-quiz/quizalto)" && git fetch origin PR-275 && git checkout PR-275
brew install quizalto
```

Branch: PR-275
